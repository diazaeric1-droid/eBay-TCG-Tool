# Copy to `.streamlit/secrets.toml`. Streamlit loads this automatically.
# (Alternatively set the same names as environment variables — see .env.example.)

# ANTHROPIC_API_KEY = "sk-ant-..."
# ANTHROPIC_MODEL = "claude-opus-4-8"

# EBAY_CLIENT_ID = "..."
# EBAY_CLIENT_SECRET = "..."
# EBAY_ENV = "production"
# EBAY_MARKETPLACE = "EBAY_US"
